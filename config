# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so feel free to edit it as you like.
# If you change your keyboard layout later, delete this file and re-run i3-config-wizard(1).

# i3 config file (v4)
# Refer to http://i3wm.org/docs/userguide.html for complete documentation!

# ===== CAT THEME =====

set $rosewater #f5e0dc
set $flamingo #f2cdcd
set $pink #f5c2e7
set $mauve #cba6f7
set $red #f38ba8
set $maroon #eba0ac
set $peach #fab387
set $yellow #f9e2af
set $green #a6e3a1
set $teal #94e2d5
set $sky #89dceb
set $sapphire #74c7ec
set $blue #89b4fa
set $lavender #b4befe
set $text #cdd6f4
set $subtext1 #bac2de
set $subtext0 #a6adc8
set $overlay2 #9399b2
set $overlay1 #7f849c
set $overlay0 #6c7086
set $surface2 #585b70
set $surface1 #45475a
set $surface0 #313244
set $base #1e1e2eCC
set $mantle #181825
set $crust #11111b

# target                 title     bg    text   indicator  border
client.focused           $lavender $base $text  $rosewater $lavender
client.focused_inactive  $overlay0 $base $text  $rosewater $overlay0
client.unfocused         $overlay0 $base $text  $rosewater $overlay0
client.urgent            $peach    $base $peach $overlay0  $peach
client.placeholder       $overlay0 $base $text  $overlay0  $overlay0
client.background        $base

# ===== END THEME =====

# Define names for default workspaces
set $ws1 "1"   
set $ws2 "2"    
set $ws3 "3"     
set $ws4 "4"     
set $ws5 "5"     
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# Set the modifier key (Mod4 is typically the Super/Windows key)
set $mod Mod4

# Set the background wallpaper using feh
exec_always feh --bg-fill ./Buildings.png
# Font for window titles and the bar
font pango:DejaVu Sans Mono 12

# Use Mouse+$mod to drag floating windows to their desired position
floating_modifier $mod

# Keybindings

# Start a terminal
bindsym $mod+Return exec kitty

# Close the focused window
bindsym $mod+q kill

# Launch Polybar
exec --no-startup-id polybar mybar
exec --no-startup-id ~/.config/polybar/launch.sh
exec --no-startup-id ~/.config/polybar/set_mike.sh 
exec --no-startup-id ~/.config/polybar/mike_toggle.sh 
exec --no-startup-id ~/.config/polybar/app.sh







# Lock the screen
bindsym $mod+x exec /home/$USER/.config/scripts/lock

# Start dmenu (application launcher)
bindsym $mod+d exec dmenu_run
# Alternatively, use rofi as a launcher
bindsym $mod+Shift+Return exec rofi -show drun
#Kazam screen kill 
bindsym $mod+r+x exec --no-startup-id pkill -f kazam

# Change focus using Vim-like keys
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# Change focus using arrow keys
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# Move using arrow keys
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Split windows
bindsym $mod+h split h        # Horizontal split
bindsym $mod+v split v        # Vertical split

# Fullscreen toggle for focused container
bindsym $mod+f fullscreen

# Change container layout
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# Toggle floating mode for the focused window
bindsym $mod+Shift+space floating toggle

# Change focus between tiling and floating windows
bindsym $mod+space focus mode_toggle

# Focus parent container
bindsym $mod+a focus parent

# Workspace switching using variables
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# Move focused container to workspace using variables
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# Reload and restart i3
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart 
#Screen shot using flameshot
bindsym --release Mod4+Print exec --no-startup-id flameshot gui
# Exit i3 with a confirmation prompt
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# Window resizing mode
mode "resize" {
    bindsym j resize shrink width 10 px or 10 ppt
    bindsym k resize grow height 10 px or 10 ppt
    bindsym l resize shrink height 10 px or 10 ppt
    bindsym semicolon resize grow width 10 px or 10 ppt

    # Arrow key bindings for resizing
    bindsym Left resize shrink width 10 px or 10 ppt
    bindsym Down resize grow height 10 px or 10 ppt
    bindsym Up resize shrink height 10 px or 10 ppt
    bindsym Right resize grow width 10 px or 10 ppt

    # Exit resize mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# Enter resize mode
bindsym $mod+r mode "resize"

# i3bar configuration
# i3bar configuration
#bar {
 #   status_command i3status -c ~/.i3status.conf
  #  position top

   # colors {
    #    background         $base
     #   statusline         $text
      #  focused_statusline $text
       # focused_separator  $base

        # Define colors for workspaces
        #focused_workspace   $mauve    $base     $text
        #active_workspace    $surface2 $base     $text
        #inactive_workspace  $base     $base     $text
        #urgent_workspace    $red      $base     $text
    #}
#}


# Transparency with picom compositor
exec_always picom -f

# Color schemes for windows 
set $bgcolor    #523d64
set $in-bgcolor #363636
set $text       #ffffff
set $u-bgcolor  #ff0000
set $indicator  #a8a3c1
set $in-text    #969696

client.focused          $bgcolor        $bgcolor        $text           $indicator
client.unfocused        $in-bgcolor     $in-bgcolor     $in-text        $in-bgcolor
client.focused_inactive $in-bgcolor     $in-bgcolor     $in-text        $in-bgcolor
client.urgent           $u-bgcolor      $u-bgcolor      $text           $u-bgcolor

# Key bindings for Media control keys
bindsym XF86AudioPlay exec playerctl play
bindsym XF86AudioPause exec playerctl pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl

